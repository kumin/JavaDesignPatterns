package com.kumin;

public class Constants {
    public enum Directions {
        Left,
        Right,
    }

    public enum Limbs {
        Arm,
        Leg
    }

    public enum Sensor {
        Temperature,
        Obstacle
    }
}
